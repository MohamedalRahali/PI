<form method="post" action="{{ path('app_deleteBlog', {'id': blog.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ce blog ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ blog.id) }}">
    <button class="btn">Delete</button>
</form>