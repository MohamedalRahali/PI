<form method="post" action="{{ path('app_deleteType', {'id': typeB.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ce type ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ typeB.id) }}">
    <button class="btn">Delete</button>
</form>