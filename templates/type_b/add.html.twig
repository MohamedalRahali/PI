{% extends 'type_b/base.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        .create-type-section {
            padding: 50px 0;
            background-color: rgba(240, 240, 240, 0.9);
            border-radius: 15px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .create-type-card {
            border: none;
            border-radius: 10px;
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.1);
        }

        .create-type-card .card-title {
            font-size: 2rem;
            color: white;
            margin-bottom: 20px;
            background-color: #007BFF; /* Couleur bleue pour le titre */
            padding: 10px;
            border-radius: 5px;
            display: inline-block;
        }

        .create-type-form {
            margin-top: 20px;
        }

        .create-type-form .form-group {
            margin-bottom: 1.5rem;
        }

        .create-type-form label {
            font-weight: bold;
            color: #333;
        }

        .create-type-form input,
        .create-type-form select,
        .create-type-form textarea {
            border: 1px solid #ced4da;
            border-radius: 5px;
            padding: 10px;
            font-size: 1rem;
            width: 100%;
        }

        .create-type-form button {
            background-color: #007BFF;
            border: none;
            padding: 10px 20px;
            font-size: 1.2rem;
            border-radius: 5px;
            transition: background-color 0.3s;
            color: white;
        }

        .create-type-form button:hover {
            background-color: #0056b3;
        }
    </style>
{% endblock %}

{% block body %}
<section class="create-type-section">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card create-blog-card">
                    <div class="card-body">
                        <h1 class="card-title text-center">Créer un type</h1>
                        {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
                            {% for child in form %}
                                <div class="form-group">
                                    {{ form_label(child, null, {'label_attr': {'class': 'form-label'}}) }}
                                    {{ form_widget(child, {'attr': {'class': 'form-control'}}) }}
                                </div>
                            {% endfor %}
                            <div class="text-center">
                                <button type="submit" class="btn btn-primary btn-lg">Enregistrer</button>
                            </div>
                        {{ form_end(form) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
{% endblock %}