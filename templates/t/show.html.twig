{% extends 'base.html.twig' %}

{% block title %}T{% endblock %}

{% block body %}
    <h1>T</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ t.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ t.name }}</td>
            </tr>
            <tr>
                <th>Desc_event</th>
                <td>{{ t.descEvent }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_t_index') }}">back to list</a>

    <a href="{{ path('app_t_edit', {'id': t.id}) }}">edit</a>

    {{ include('t/_delete_form.html.twig') }}
{% endblock %}
