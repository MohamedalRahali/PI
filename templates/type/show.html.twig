{% extends 'base.html.twig' %}

<<<<<<< HEAD
{% block title %}Show Type{% endblock %}

{% block body %}
    <h1>Type Details</h1>

    <table>
        <tr>
            <th>Id</th>
            <td>{{ type.id }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ type.name }}</td>
        </tr>
        <tr>
            <th>Description</th>
            <td>{{ type.descEvent }}</td>
        </tr>
    </table>

    <a href="{{ path('app_type_edit', {'id': type.id}) }}" class="btn btn-warning">Edit</a>
    <a href="{{ path('app_type_index') }}" class="btn btn-secondary">Back to list</a>
{% endblock %}
=======
{% block title %}Détails du Type{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="card shadow-lg p-4">
            <h1 class="mb-4 text-center text-primary">
                <i class="fas fa-info-circle"></i> Détails du Type
            </h1>

            <div class="type-details">
                <p><strong><i class="fas fa-id-badge"></i> ID:</strong> {{ t.id }}</p>
                <p><strong><i class="fas fa-heading"></i> Nom:</strong> {{ t.name }}</p>
                <p><strong><i class="fas fa-info-circle"></i> Description:</strong> {{ t.descEvent }}</p>
                <p><strong><i class="fas fa-calendar-day"></i> Événement associé:</strong> 
                    {% if t.evenment and t.evenment.types is not empty %}
    <h3 class="mt-4">Autres types liés à cet événement :</h3>
    {% for type in t.evenment.types %}
        <div class="form-container mt-4 text-center">
            <p><strong>Nom :</strong> {{ type.name }}</p>
            <a href="{{ path('app_type_edit', {'id': type.id}) }}" class="btn btn-primary">
                <i class="fas fa-edit"></i> Modifier
            </a>
            <form method="post" action="{{ path('app_type_delete', {'id': type.id}) }}" class="mt-2">
                <button class="btn btn-danger">
                    <i class="fas fa-trash-alt"></i> Supprimer
                </button>
                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type.id) }}">
            </form>
        </div>
    {% endfor %}
{% endif %}


    </div>
{% endblock %}
>>>>>>> cd5b4cd (crud tout en marche)
